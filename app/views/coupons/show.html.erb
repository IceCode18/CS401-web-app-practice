<% title "Coupon Info" %>
<%= link_to "", pass_coupons_path(@pass), class: "btn btn-secondary f-right lnk-back" %>

<% if @show_title %>
  <br>
    <div id="title" class="row">
      <div class = "col">
        <h1>Guest <%= yield(:title) %></h1>
      </div>
    </div>
    <br>
<% end %>

<hr>
<br>

<%= render @coupon %>


<%= link_to "Edit", edit_pass_coupon_path(@pass, @coupon), class: "btn btn-info" %>
<%= link_to "Delete", pass_coupon_path(@pass, @coupon), method: :delete, data: {confirm: "Are you sure you want to delete this?" },  class: "btn btn-info" %>
